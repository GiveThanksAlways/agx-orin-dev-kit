[package]
name = "hcq-replay"
version = "0.1.0"
edition = "2021"
description = "Rust hot path for replaying tinygrad HCQGraph via raw MMIO doorbell on Tegra"
license = "MIT"

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
